---
import Layout from "../../layouts/Layout.astro";
import * as Plot from "@observablehq/plot";
import PlotFigure from "../../components/PlotFigure.astro";
import penguins from "../../data/penguins.json";
---

<Layout
  title="Les manchots à jugulaire"
  description="Les manchots à jugulaire selon leur sexe."
>
  <h1>Les manchots à jugulaire</h1>
  <p>
    Voici la différence de taille des nageoires des manchots à jugulaire selon
    leur sexe.
    <br />La différence est subtile, on retrouve des valeurs équivalentes.
  </p>
  <h3>Manchots mâles</h3>
  <PlotFigure
    options={{
      marks: [
        Plot.dot(
          penguins.filter(
            (penguin) =>
              penguin.species === "Chinstrap" && penguin.sex === "MALE",
          ),
          { x: "culmen_length_mm", y: "culmen_depth_mm", stroke: "purple" },
        ),
      ],
    }}
  />
  <h3>Manchots femelles</h3>
  <PlotFigure
    options={{
      marks: [
        Plot.dot(
          penguins.filter(
            (penguin) =>
              penguin.species === "Chinstrap" && penguin.sex === "FEMALE",
          ),
          { x: "culmen_length_mm", y: "culmen_depth_mm", stroke: "orange" },
        ),
      ],
    }}
  />
</Layout>
